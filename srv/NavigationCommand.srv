# NavigationCommand.srv
# 导航控制命令
string command       # 命令类型: nav_to, pause, resume, stop, clear
string target_type   # 目标类型: waypoint, index, pose (当command为nav_to时使用)
string[] params      # 参数列表: 例如航点名/索引/位姿坐标等
---
bool success         # 执行结果
string message       # 结果信息